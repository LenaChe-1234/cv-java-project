name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      test_subset:
        description: 'Which tests to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - api
          - ui
      browser:
        description: 'Browser for UI tests'
        required: true
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
          - edge
      base_url:
        description: 'Application base URL'
        required: true
        default: 'https://juice-shop.herokuapp.com'
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        subset: [api, ui]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Run API tests
        if: >-
          matrix.subset == 'api' &&
          (github.event_name != 'workflow_dispatch' || github.event.inputs.test_subset == 'all' || github.event.inputs.test_subset == 'api')
        run: |
          mvn -B -Dtest=*ApiTests test \
            -Durl=${{ github.event.inputs.base_url || 'https://juice-shop.herokuapp.com' }}

      - name: Run UI tests headless
        if: >-
          matrix.subset == 'ui' &&
          (github.event_name != 'workflow_dispatch' || github.event.inputs.test_subset == 'all' || github.event.inputs.test_subset == 'ui')
        run: |
          xvfb-run -a mvn -B -Dtest=*UiTest,*UiTests test \
            -Durl=${{ github.event.inputs.base_url || 'https://juice-shop.herokuapp.com' }} \
            -Dbrowser=${{ github.event.inputs.browser || 'chrome' }} \
            -Dheadless=true

      - name: Generate Allure report
        if: always()
        run: mvn -B allure:report

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ matrix.subset }}
          path: target/site/allure-maven-plugin
          if-no-files-found: ignore

      - name: Upload Selenide reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: selenide-reports-${{ matrix.subset }}
          path: target/selenide-reports
          if-no-files-found: ignore

  zap-baseline:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: ${{ github.event.inputs.base_url || 'https://juice-shop.herokuapp.com' }}
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'
